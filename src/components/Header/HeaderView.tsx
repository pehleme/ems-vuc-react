import { AppBar, Toolbar } from "@mui/material";

function HeaderView() {
  return (
    <header>
      <AppBar position="static">
        <Toolbar>Teste</Toolbar>
      </AppBar>
    </header>
  );
}

export { HeaderView };
