import { AppBar, Toolbar } from "@mui/material";

function HeaderView() {
  return (
    <header>
      <AppBar>
        <Toolbar>Teste</Toolbar>
      </AppBar>
    </header>
  );
}

export { HeaderView };
